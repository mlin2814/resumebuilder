{{!-- <div class="container">
  <div class="row">
    <div class="col-md-12">
      <div class=".img-responsive">
          <img src="assets/img/burger-1674881_960_720.png" alt="Burger" style="width:250px; height: 250px;" align="center">
      </div>
          <h1>Eat Da Burger!</h1>
    </div>
  </div> --}}

<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h2>Positions</h2>
    
        <ul>
          {{#each resumes}}
              <li>
                {{this.resume_name}}

                <form action="resume/update/{{this.id}}?_method=PUT" method="POST">
                  <input type="hidden" name="Selected" value="true">
                  <button type="submit">Select this Position!</button>
                </form>  
              </li>
          {{/each}}
        </ul>
    </div>
    <div class="col-md-6">
      <h2>Positions Desired</h2>

      <ul>
        {{#each resumes}}
          {{#if this.selected}}
            <li>
              {{this.resume_name}}

              <form action="resume/update/{{this.id}}?_method=PUT" method="POST">
                <input type="hidden" name="Selected" value="false">
              </form>

            </li>
          {{/if}}
        {{/each}}
      </ul>
      
    </div>
  </div>
</div>

    <h2>Add a Position!</h2>
    <form class="create-update-form" action="/resume/create" method="POST">

      <div class="form-group">
        <label for="re">Burger Name:</label>
        <input type="text" id="bu" name="burger_name">
      </div>
      <div class="form-group">
        <input type="hidden" name="devoured" value="1" checked>
        
      </div>
      <button type="submit">Add da Burger!</button>
    </form>
</div>